{
    "name": "app-tareas-webapp",
    "type": "module",
    "scripts": {
        "dev": "vite dev",
        "build": "vite build",
        "preview": "$npm_execpath run build && vite preview",
        "deploy": "$npm_execpath run build && wrangler deploy",
        "cf-typegen": "wrangler types --env-interface CloudflareBindings",
        "test": "vitest",
        "drizzle-kit-test": "DATABASE_URL=postgresql://esteban:55Cmalo_postgres@localhost:5433/postgres_test npx drizzle-kit"
    },
    "dependencies": {
        "@hono/zod-validator": "^0.7.6",
        "@types/pg": "^8.16.0",
        "drizzle-orm": "^0.45.1",
        "hono": "^4.11.3",
        "pg": "^8.16.3",
        "zod": "^4.3.2"
    },
    "devDependencies": {
        "@cloudflare/vite-plugin": "^1.2.3",
        "@types/node": "^25.0.3",
        "dotenv": "^17.2.3",
        "drizzle-kit": "^0.31.8",
        "tsx": "^4.21.0",
        "vite": "^6.3.5",
        "vite-ssr-components": "^0.5.2",
        "vitest": "^4.0.16",
        "wrangler": "^4.17.0"
    }
}